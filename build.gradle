apply plugin: 'java'
apply plugin: 'application' // Gradle can only run on Java version 7 or higher. Gradle 3.x still supports compiling, testing, generating Javadoc and executing applications for Java 6. Java 5 is not supported.
// apply plugin: 'eclipse'

mainClassName = "com.github.iookill.logfilter.LogFilterMain"

repositories {
    mavenCentral()
}

dependencies {
    // compile 'log4j:log4j:1.2.17'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

//Run: gradle run or  build/distributions/LogFilter/bin/LogFilter &
//Realease: gradle build distZip